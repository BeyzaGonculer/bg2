import 'package:flutter/material.dart';

class buk extends StatelessWidget {
  const buk({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("aaabbbb"),
    );
  }
}
